# SERVER-STATS CONFIGURATION EXAMPLE
# This file shows how to customize thresholds and settings
# Copy and modify these values in the main script

#############################################
# THRESHOLD SETTINGS
#############################################

# CPU Usage Thresholds (percentage)
CPU_WARNING_THRESHOLD=80
CPU_CRITICAL_THRESHOLD=90

# Memory Usage Thresholds (percentage)
MEMORY_WARNING_THRESHOLD=80
MEMORY_CRITICAL_THRESHOLD=90

# Disk Usage Thresholds (percentage)
DISK_WARNING_THRESHOLD=80
DISK_CRITICAL_THRESHOLD=90

# Load Average Threshold (compared to CPU cores)
LOAD_AVERAGE_MULTIPLIER=1.5

#############################################
# DISPLAY SETTINGS
#############################################

# Number of top processes to show
TOP_PROCESSES_COUNT=5

# Include swap information (true/false)
SHOW_SWAP_INFO=true

# Include network statistics (true/false)
SHOW_NETWORK_STATS=true

# Include security logs (true/false)
SHOW_SECURITY_LOGS=true

# Show largest directories (true/false)
SHOW_LARGE_DIRECTORIES=true

#############################################
# COLOR SETTINGS
#############################################

# Enable colors (true/false)
ENABLE_COLORS=true

# Color codes (modify as needed)
# RED='\033[0;31m'
# GREEN='\033[0;32m'
# YELLOW='\033[1;33m'
# BLUE='\033[0;34m'
# PURPLE='\033[0;35m'
# CYAN='\033[0;36m'
# NC='\033[0m'

#############################################
# LOG FILE PATHS
#############################################

# Authentication log files to check
AUTH_LOG_PATHS=(
    "/var/log/auth.log"
    "/var/log/secure"
    "/var/log/messages"
)

# Maximum number of failed login attempts to display
MAX_FAILED_LOGINS_DISPLAY=5

#############################################
# MONITORING INTERVALS
#############################################

# For continuous monitoring (if implemented)
REFRESH_INTERVAL=30  # seconds

# History to keep for trending (if implemented)
HISTORY_DAYS=7

#############################################
# OUTPUT SETTINGS
#############################################

# Output format options
OUTPUT_FORMAT="console"  # console, json, csv
TIMESTAMP_FORMAT="%Y-%m-%d %H:%M:%S"

# Report generation
GENERATE_REPORT=false
REPORT_DIRECTORY="/var/log/server-stats"

#############################################
# NOTIFICATION SETTINGS
#############################################

# Email notifications (if implemented)
ENABLE_EMAIL_ALERTS=false
ALERT_EMAIL="admin@example.com"
SMTP_SERVER="localhost"

# Slack notifications (if implemented)
ENABLE_SLACK_ALERTS=false
SLACK_WEBHOOK_URL=""

#############################################
# USAGE EXAMPLES
#############################################

# To use these settings, you would modify the main script like this:
#
# 1. Load configuration at the top of the script:
#    source ./server-stats.conf 2>/dev/null || true
#
# 2. Use variables instead of hardcoded values:
#    if (( $(echo "$total_cpu_usage > $CPU_WARNING_THRESHOLD" | bc -l) )); then
#
# 3. Apply color settings:
#    if [ "$ENABLE_COLORS" = "true" ]; then
#        # Use color codes
#    else
#        # Disable colors
#    fi

#############################################
# CUSTOMIZATION NOTES
#############################################

# 1. Threshold Customization:
#    - Lower thresholds for more sensitive alerting
#    - Higher thresholds for less noisy environments
#
# 2. Display Customization:
#    - Disable features not needed in your environment
#    - Adjust the number of processes shown
#
# 3. Performance Tuning:
#    - Disable expensive operations for faster execution
#    - Adjust refresh intervals for real-time monitoring
#
# 4. Security Considerations:
#    - Limit log file access based on permissions
#    - Consider rotating large log files
#
# 5. Integration:
#    - Modify output format for monitoring tools
#    - Add custom metrics specific to your infrastructure
